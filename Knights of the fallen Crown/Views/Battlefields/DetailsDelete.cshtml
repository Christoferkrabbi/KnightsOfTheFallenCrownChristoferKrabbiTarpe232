@model KnightsOfTheFallenCrown.Models.Battlefields.BattlefieldDetailsDeleteViewModel
 @if (ViewData["RequestedView"] == "Details")
{
    <h1 class="gTZRG">@Model.BattlefieldName</h1>
    ViewData["IsAnimated"] = "false";
}
else
{
    <h1 class="gTZRG">Delete @Model.BattlefieldName ?</h1>
    ViewData["IsAnimated"] = "true";
}
<div class="gTZRG gTB">
    <form method="post" enctype="multipart/form-data">
        <input asp-for="ID" type="hidden" />
        <input asp-for="CreatedAt" type="hidden" />
        <input asp-for="ModifiedAt" type="hidden" />
        <table>
            <thead>
                <tr>
                    <th rowspan="5">
                        @foreach (var image in Model.Image)
                        {
                            if (image.ImageTitle == "LongViewAnimated.gif" && ViewData["IsAnimated"] == "true")
                            {
                                <partial name="_Images" model="image" />                                
                            }
                            if (image.ImageTitle == "LongViewStatic.png" && ViewData["IsAnimated"] == "false")
                            {
                                <partial name="_Images" model="image" />
                            }
                        }
                    </th>
                    <th>@Model.BattlefieldName</th>
                </tr>
                <tr>
                    <td>@Model.BattlefieldType</td>
                </tr>
                <tr>
                    <td>@Model.Settlements</td>
                </tr>
                <tr>
                    <td>@Model.TechnicalLevel</td>
                </tr>
                <tr>
                    <td>Knights of type @Model.EnvironmentBoost are boosted</td>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td colspan="2">@Model.BattlefieldDescription</td>
                </tr>
                <tr>
                    <td>currentowner</td>
                    <td>@Model.LordWhoDominatesThisBattlefield</td>
                </tr>
                <tr>
                    <td>whereis</td>
                    <td>@Model.ContinentID</td>
                </tr>
                <tr>
                    <td>actions</td>
                    <td>
                        @if (ViewData["RequestedView"] == "Details")
                        {
                            <a asp-action="index" class="btn btn-primary">Back</a>
                        }
                        else
                        {
                            <input asp-action="DeleteConfirmation"
                                   asp-controller="Battlefields"
                                   type="submit"
                                   class="btn btn-primary"
                                   value="Thanos snap" />
                            <a asp-action="index" class="btn btn-primary">Cancel</a>
                        }
                    </td>
                </tr>
            </tbody>
        </table>
    </form>
</div>